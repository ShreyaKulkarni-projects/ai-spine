{
  "name": "@ai-spine/mcp-server",
  "version": "0.1.0",
  "description": "Local stdio MCP server exposing AI Spine's sycophancy detection as a tool for Claude Desktop and Claude Code.",
  "type": "module",
  "bin": {
    "ai-spine-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@ai-spine/core": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  },
  "license": "MIT"
}
