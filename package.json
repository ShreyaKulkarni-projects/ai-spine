{
  "name": "ai-spine",
  "private": true,
  "version": "0.1.0",
  "description": "Does this AI actually hold its ground, or does it just fold to agree with you?",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspace=@ai-spine/core",
    "eval": "npm run eval --workspace=@ai-spine/core",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
